/*
 1) In interfaces, the properties are automatically "public static final", this is because, if it is not static, we need to create an instance of it before using it
    . It if final to have uniformity across all its usage.
 */

public interface Interfaces {

    public static final int variable=5;
}
